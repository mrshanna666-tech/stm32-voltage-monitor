# STM32 电压监控界面实现思路

## 1. 实现目标

本次改造的目标是把 Figma 中的 `STM32 Voltage Monitor / Desktop / Overview` 页面落到现有 PySide6 项目里，同时保留原项目已经具备的功能：

- 每 500 ms 获取一次模拟串口数据；
- 解析 `VOLT=2.36` 形式的数据；
- 保留最近 50 个采样点；
- 计算当前值、平均值和最大值；
- 2.5 V 及以上触发告警；
- 实时更新 Matplotlib 曲线；
- 自动把采样结果写入 `voltage_data.csv`；
- 支持开始、停止和再次开始采集。
- 支持“模拟模式 / 串口模式”切换；没有 COM 口时显示错误状态但不退出程序。

Figma 设计源：[STM32 电压采集与监控系统](https://www.figma.com/design/5gUwaaByGkmrdu3w1v5d5i?node-id=3-9)

## 2. 现有项目分析

原项目已经把数据流程拆成了几个简单模块：

- `simulator.py`：生成模拟电压；
- `data_source.py`：把模拟器和真实串口包装成相同的读取接口；
- `parse.py`：校验并解析电压字符串；
- `recorder.py`：追加写入 CSV；
- `gui.py`：负责定时采集、统计、曲线和按钮交互。

因此没有重新设计数据层，而是把主要工作集中在 `gui.py` 的视图结构和状态呈现上。这样可以降低改动风险，也便于以后把 `simulator.create_data()` 替换成真实串口读取函数。

## 3. Figma 到 PySide6 的映射

| Figma 区域 | PySide6 实现 | 说明 |
| --- | --- | --- |
| 1440×1024 页面 | `VoltageMonitor` + 根布局 | 默认窗口尺寸与设计稿一致，较小窗口可通过滚动区域访问全部内容 |
| 顶部产品栏 | `QFrame#appHeader` | 包含品牌图标、标题、数据源、设备和连接状态 |
| 四张指标卡 | `MetricCard` | 当前电压、平均电压、最大电压和采集状态统一封装 |
| 实时曲线卡片 | `VoltageChartPanel` | Qt 容器负责标题和图例，Matplotlib 负责数据曲线、面积、阈值线和当前值标记 |
| 采集控制卡片 | `CollectionControlPanel` | 参数信息、硬件提示、开始与停止按钮 |
| 最新采样记录 | `SamplesPanel` + `SampleTable` | 展示最近四条记录，并对告警行使用红色语义色 |
| 状态标签 | `StatusPill` | 统一采集中、已停止和 CSV 记录状态的样式 |

设计尺寸按 48 px 水平页面边距、32 px 垂直边距、24 px 区块间距实现。中部使用自适应图表列加 408 px 固定控制列，在 1440 px 窗口下还原成设计稿中的 916/408 双栏比例。

## 4. 样式与资源策略

### 字体

设计使用 Noto Sans SC。为了不依赖用户电脑是否已经安装字体，项目把 Google Fonts 官方的可变字体放在：

`assets/fonts/NotoSansSC-VariableFont_wght.ttf`

程序启动时使用 `QFontDatabase.addApplicationFont()` 加载。许可证保存在 `assets/fonts/OFL.txt`。

### 图标

标题栏的电压波形和绿色状态圆点使用从 Figma 导出的原始 SVG：

- `assets/voltage-wave.svg`
- `assets/status-dot.svg`

这样可以避免用文字字符或手工绘制图形近似设计资源。

### 颜色与组件

`COLORS` 字典集中保存页面背景、卡片、正文、次级文字、边框、成功和告警颜色。Qt 样式统一由 `build_style_sheet()` 生成，动态状态使用控件属性 `tone` 切换，而不是在业务逻辑里反复拼接整段样式表。

卡片阴影通过 `QGraphicsDropShadowEffect` 实现；边框、圆角和间距则通过 Qt Style Sheet 与布局参数实现。

## 5. 数据与状态流

程序启动后的流程如下：

1. 读取 `voltage_data.csv` 中最近 50 条有效记录；
2. 用历史数据初始化指标、曲线和最近记录表；
3. 默认选择模拟模式并自动进入采集状态；
4. 定时器每 500 ms 调用 `update_data()`；
5. 当前数据源通过统一的 `read()` 方法提供原始数据，解析器校验电压范围；
6. 数据进入 `deque(maxlen=50)`，超过长度后自动丢弃最早记录；
7. 更新指标卡、曲线、告警点、表格和最后更新时间；
8. 通过 `save_data()` 写入 CSV；
9. 开始/停止按钮同步更新定时器、按钮可用状态和界面状态标签。

告警状态没有通过改变整条曲线颜色来表达，而是保持深色主曲线，并用红色采样点、红色数值和红色阈值线突出异常。这与 Figma 中的视觉层级一致，也避免一次告警让整张图失去连续性。

## 6. 可测试性设计

界面支持 `VOLTAGE_MONITOR_SNAPSHOT=1` 快照模式。该模式会载入设计稿对应的固定数据，不启动真实定时采集，方便稳定地做截图和视觉回归测试。

`render_preview.py` 会自动启用离屏渲染和快照模式，并生成：

`artifacts/implementation.png`

这让界面可以在没有显示器或不弹出窗口的环境下完成 1440×1024 视觉验证。

## 7. 运行方式

启动程序：

```powershell
python gui.py
```

重新生成视觉验收截图：

```powershell
python render_preview.py
```

## 8. 模拟与串口双模式

双模式已经通过 `data_source.py` 接入。`SimulatorSource` 和 `SerialSource` 都提供 `open()`、`read()`、`close()`，所以 GUI 不再关心数据来自随机模拟还是 COM 口。

更详细的逐行思路、切换顺序、异常处理和硬件接线说明见：

[模拟与串口模式实现讲解.md](模拟与串口模式实现讲解.md)

当前电脑未检测到 COM 口，因此已经完成模拟模式与“无串口时不崩溃”的验证；真实 USB 转 TTL 到货后，只需要在页面中选择串口模式、刷新端口并开始采集。

