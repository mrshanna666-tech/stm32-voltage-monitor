# 模拟 / 串口模式实现讲解

这份文档按“为什么这样设计 → 每个类做什么 → GUI 怎样切换 → 数据怎样流动 → 出错时怎么办”的顺序讲解。即使你刚开始接触 PySide6，也可以从头跟着理解。

## 一、我们到底要解决什么问题

原来的 GUI 在 `update_data()` 中直接调用：

```python
create_data()
```

这意味着 GUI 和模拟器绑在了一起。以后接入 STM32 时，如果直接把这行改成串口代码，界面类就会同时负责：

- 打开和关闭 COM 口；
- 读取字节；
- 处理串口异常；
- 解析电压；
- 更新界面；
- 保存 CSV。

职责太多以后，代码会越来越难改，也很难单独测试。

GPT Work 建议增加一个“数据源层”。可以把它想成两个不同的水龙头：

- 模拟器是第一个水龙头；
- STM32 串口是第二个水龙头；
- GUI 只拿同一个杯子接水，不关心水来自哪根管道。

在代码里，这个“统一接水动作”就是：

```python
raw_data = self.data_source.read()
```

现在的数据流是：

```text
SimulatorSource ─┐
                 ├─ read() → parse_data() → save_data()
SerialSource ────┘                         ↓
                                 统计、曲线、表格、告警
```

最重要的好处是：从模拟模式切换成串口模式以后，解析、CSV、统计和图表代码一行都不需要重写。

## 二、文件如何分工

### `data_source.py`

只负责“原始数据从哪里来”。它不知道界面上有什么卡片，也不负责画图。

里面主要有：

- `SimulatorSource`：调用原来的模拟器；
- `SerialSource`：使用 pyserial 读取 COM 口；
- `list_serial_devices()`：列出 Windows 检测到的串口；
- `create_source()`：根据模式创建正确的数据源；
- `DataSourceError`：把底层异常转换成适合显示给用户的错误。

### `gui.py`

负责：

- 模式选择框；
- 串口选择框和刷新按钮；
- 开始、停止和模式切换；
- 调用当前数据源的 `read()`；
- 解析、CSV、统计、曲线和状态显示。

### 原来的模块

这些接口继续保留：

- `simulator.py` 生成 `VOLT=2.35`；
- `parse.py` 把字符串解析成浮点数；
- `recorder.py` 写入 CSV。

这就是“只增加一层，不推翻原有功能”。

## 三、为什么两个数据源要有相同的方法

两个类都实现三个动作：

```python
open()
read()
close()
```

它们代表一个数据源完整的生命周期。

### `open()`：开始前准备

- 模拟模式不需要硬件，所以什么都不做；
- 串口模式会打开指定 COM 口、设置波特率和读取超时。

### `read()`：读取一条原始数据

- 模拟模式返回 `simulator.create_data()` 的结果；
- 串口模式调用 `readline()`，读取一整行 STM32 数据。

两种模式返回的格式保持一致：

```text
VOLT=2.35
```

### `close()`：停止时释放资源

- 模拟模式没有资源需要释放；
- 串口模式会关闭 COM 口，让别的程序能够再次使用它。

如果停止采集却不关闭串口，下一次运行程序时可能出现“端口被占用”。所以 `close()` 不是可有可无的清理动作。

## 四、`SimulatorSource` 为什么仍然有 open 和 close

模拟器实际上不需要打开或关闭，但仍然保留这两个空方法：

```python
class SimulatorSource:
    def open(self):
        return None

    def read(self):
        return create_data()

    def close(self):
        return None
```

这样 GUI 可以统一写成：

```python
self.data_source.open()
raw_data = self.data_source.read()
self.data_source.close()
```

GUI 不需要再写：

```python
if 是模拟模式:
    ...
else:
    ...
```

这种“不同对象提供相同操作”的思路叫多态。现在不必死记术语，只要记住：接口相同，使用者就可以少写很多分支判断。

## 五、`SerialSource` 是怎样工作的

创建对象时先保存配置：

```python
SerialSource(port="COM5", baudrate=115200, timeout=0.08)
```

参数含义：

- `port`：Windows 串口名，例如 `COM5`；
- `baudrate`：通信速度，必须和 STM32 配置一致；
- `timeout`：最多等待多少秒。

### 为什么超时只设 0.08 秒

PySide6 的 `QTimer` 每 500 ms 调用一次 `update_data()`。如果 `readline()` 永久等待，主线程就会卡住，窗口无法拖动，按钮也点不了。

设置 0.08 秒表示：暂时没有数据时，最多等约 80 ms 就返回 `None`，GUI 仍然能继续处理事件。

这适合当前较低频率的课程项目。如果以后采样频率很高，建议把串口读取移动到 `QThread` 工作线程中。

### 为什么使用 `readline()`

STM32 应当每发送一条数据就加换行：

```text
VOLT=2.35\r\n
```

`readline()` 会一直读到换行符，Python 一次就能取得一条完整消息。这样可以避免把上一条和下一条数据粘在一起。

### 字节为什么要 decode

串口收到的是字节：

```python
b"VOLT=2.35\r\n"
```

解析函数需要普通字符串，所以要转换：

```python
text = raw_bytes.decode("utf-8", errors="replace").strip()
```

- `decode()`：字节转字符串；
- `errors="replace"`：遇到损坏字节时不让程序直接崩溃；
- `strip()`：去掉末尾的 `\r\n` 和空格。

## 六、为什么 pyserial 可以是可选依赖

程序用 `try / except ImportError` 导入 pyserial：

```python
try:
    import serial
except ImportError:
    serial = None
```

这样即使某台电脑没有安装 pyserial，模拟模式仍然可以运行。只有切换到串口模式时，界面才提示：

```text
请运行：python -m pip install pyserial
```

这比程序启动时直接报 `ModuleNotFoundError` 更友好，也符合“没有硬件时继续开发软件端”的目标。

## 七、GUI 是怎样切换模式的

顶部选择框保存的不是只用于显示的文字，而是模式值：

```python
模拟模式 → simulator
串口模式 → serial
```

用户切换时，`on_source_mode_changed()` 按下面的顺序执行：

1. 记住切换前是否正在采集；
2. 停止 `QTimer`；
3. 关闭旧数据源；
4. 保存新模式；
5. 串口模式下刷新 COM 口列表；
6. 创建新的数据源对象；
7. 更新端口框、提示卡和连接状态；
8. 如果切换前正在运行，就尝试继续启动新模式。

为什么一定是“先停、再关、再换”？

因为如果定时器仍在运行，它可能在切换过程中调用旧数据源；如果旧 COM 口没有关闭，新串口也可能无法打开。这个顺序可以避免状态混乱。

## 八、开始采集时发生什么

`start_collection()` 的核心步骤是：

```python
self.configure_data_source()
self.data_source.open()
self.timer.start()
```

串口模式下，如果端口不存在或无法打开，`open()` 会抛出 `DataSourceError`。GUI 捕获后会：

- 停止定时器；
- 关闭数据源；
- 把状态卡改成“连接失败”；
- 在右侧提示具体原因；
- 保持窗口继续运行。

因此“没有 COM 口”是一个可处理的运行状态，不是让程序退出的致命错误。

## 九、`update_data()` 为什么只写一套

无论当前是什么模式，读取入口都是：

```python
raw_data = self.data_source.read()
```

取得原始字符串后，下面的流程完全共用：

```python
voltage = parse_data(raw_data)
save_data(voltage, status)
```

然后再统一更新：

- 当前、平均、最大电压；
- 2.5 V 告警状态；
- 最近 50 个曲线点；
- 最近四条采样记录；
- CSV 记录状态；
- 最后更新时间。

这正是数据源分层最有价值的地方。以后增加“读取文件模式”或“网络模式”，也只需要再写一个具有 `open/read/close` 的数据源类。

## 十、三类“没有数据”要区别处理

### 1. 暂时没收到串口行

`read()` 返回 `None`。这不是错误，界面显示“等待数据”，下一次定时器继续读取。

### 2. 收到一行，但格式错误

例如：

```text
ABC=2.35
```

`parse_data()` 返回 `None`，界面显示“解析失败”，这条数据不会进入 CSV 和曲线。

### 3. COM 口断开或被占用

`SerialSource` 抛出 `DataSourceError`。GUI 停止采集、释放端口并显示错误，但主窗口保持打开。

把这三种情况分开，用户才能知道究竟是在等待、数据格式不对，还是连接真的断了。

## 十一、页面怎样使用

### 模拟模式

1. 顶部选择“模拟模式”；
2. 点击“开始采集”；
3. 程序自动生成电压，更新曲线并写入 CSV。

模拟模式不需要 COM 口，串口端口框会显示“无需串口”。

### 串口模式

1. 连接 USB 转 TTL；
2. 顶部选择“串口模式”；
3. 点击右侧“刷新”；
4. 在下拉框选择 COM 口；
5. 点击“开始采集”。

当前电脑检测到的串口数量是 0，因此程序会显示“未发现串口”，这是正常状态。

## 十二、STM32 与 USB 转 TTL 的连接

USART1 常用引脚：

```text
STM32 PA9  (TX) → USB 转 TTL RX
STM32 PA10 (RX) → USB 转 TTL TX
STM32 GND       → USB 转 TTL GND
```

注意：

- TX 与 RX 要交叉连接；
- 两边必须共地；
- 使用 3.3 V TTL 电平，不要连接 RS-232 电平；
- 开发板已经单独供电时，通常不要再连接 USB 转 TTL 的 5V/VCC；
- ST-Link V2 负责下载和调试，不等于运行时串口；
- STM32 每条消息末尾必须发送换行。

## 十三、怎样运行和测试

启动 GUI：

```powershell
python -m pip install -r requirements.txt
python gui.py
```

查看 Windows 当前识别到的串口：

```powershell
python -m serial.tools.list_ports
```

运行数据源单元测试：

```powershell
python -m unittest discover -s tests -v
```

生成模拟模式截图：

```powershell
python render_preview.py --mode simulator
```

生成“串口模式但没有 COM 口”的截图：

```powershell
python render_preview.py --mode serial --output artifacts\serial-mode-no-port.png
```

测试中使用假的串口对象返回 `VOLT=2.35`，所以即使没有 USB 转 TTL，也能验证 `SerialSource` 的打开、读取和关闭逻辑。真实串口联调仍需要硬件到位后完成。

## 十四、建议的阅读顺序

如果你想通过这个项目学习代码，建议按下面顺序阅读：

1. 先看 `SimulatorSource`，理解统一接口；
2. 再看 `SerialSource.read()`，理解字节、换行和解码；
3. 看 `VoltageMonitor.start_collection()`，理解资源打开；
4. 看 `VoltageMonitor.update_data()`，理解统一数据管线；
5. 看 `on_source_mode_changed()`，理解切换前为什么要停止和关闭；
6. 最后运行测试，对照每个测试理解预期行为。

掌握这条主线以后，你就不只是“会运行代码”，而是能解释为什么数据源要分层、为什么资源要关闭、为什么 GUI 不能用无限循环阻塞。
